# Проверка переменных окружения

## Проблема: "Kinescope API credentials not configured"

Если вы видите эту ошибку, но файл `.env.local` создан и содержит переменные, выполните следующие шаги:

## ✅ Решение

### 1. Убедитесь, что файл `.env.local` находится в корне проекта

Файл должен быть здесь:
```
ai-site/
├── .env.local          ← ЗДЕСЬ
├── package.json
├── next.config.ts
└── ...
```

### 2. Проверьте содержимое `.env.local`

Файл должен содержать:
```bash
KINESCOPE_API_KEY=ваш_ключ
KINESCOPE_PROJECT_ID=ваш_id
```

**Важно:**
- НЕ должно быть пробелов вокруг `=`
- НЕ должно быть кавычек вокруг значений
- Каждая переменная на отдельной строке

### 3. ОБЯЗАТЕЛЬНО перезапустите сервер

**Это критически важно!** Next.js загружает переменные окружения только при старте.

1. Остановите сервер: нажмите `Ctrl+C` в терминале
2. Запустите снова:
   ```bash
   npm run dev
   ```

### 4. Проверьте логи сервера

После перезапуска в терминале должны появиться логи:
```
🎬 Kinescope API /get-video-url called
🔍 Environment check: { hasApiKey: true, hasProjectId: true, ... }
```

Если видите `hasApiKey: false` или `hasProjectId: false` - переменные не загружены.

## 🔍 Диагностика

### Проверка в терминале сервера

После перезапуска откройте страницу урока и посмотрите в терминал сервера. Должны увидеть:
```
🔍 Environment check: {
  hasApiKey: true,
  hasProjectId: true,
  apiKeyLength: 36,
  projectIdLength: 36
}
```

### Если переменные все еще не загружаются

1. Проверьте, что файл называется именно `.env.local` (не `.env`, не `.env.local.txt`)
2. Убедитесь, что нет скрытых символов в начале/конце значений
3. Попробуйте пересоздать файл заново
4. Проверьте, что файл не в `.gitignore` (но это нормально, он не должен быть в git)

## 📝 Пример правильного `.env.local`

```bash
KINESCOPE_API_KEY=ffc7766b-749e-4d82-851f-b413ccd4e0d1
KINESCOPE_PROJECT_ID=9ffe2fac-6904-450d-8da1-5413fa20a65a
NEXT_PUBLIC_SUPABASE_URL=https://yourproject.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-anon-key
```

## ⚠️ Частые ошибки

1. **Файл создан, но сервер не перезапущен** - самая частая причина
2. **Пробелы в значениях** - `KINESCOPE_API_KEY = value` (неправильно)
3. **Кавычки вокруг значений** - `KINESCOPE_API_KEY="value"` (неправильно)
4. **Файл в неправильной папке** - должен быть в корне проекта